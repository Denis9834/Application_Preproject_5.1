spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/database_SpringSecurity
    username: postgres
    password: postgre
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
